{
  "name": "zachs-bs-hub",
  "version": "1.0.0",
  "private": true,
  "description": "Charcoal + royal purple portfolio / navigational hub. Modular Node + React/TS app.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "install:all": "npm install --include=dev --prefix server && npm install --include=dev --prefix client",
    "dev": "concurrently -n server,client -c magenta,cyan \"npm:dev:server\" \"npm:dev:client\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "build": "npm run install:all && npm run build --prefix client && npm run build --prefix server",
    "start": "npm run start --prefix server",
    "seed": "npm run seed --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
